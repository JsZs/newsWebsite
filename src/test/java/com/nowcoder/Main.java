package com.nowcoder;

import java.util.*;
/*
public class Main {

    public static void testArrange() {
        System.out.println("请输入所有数据");
        Scanner scA = new Scanner(System.in);
        Scanner scB = new Scanner(System.in);
        List<String> listA = new ArrayList<String>();

        while(scA.hasNext()) {
            String str = scA.nextLine();
            if(str.equals("exit")) {
                break;
            }
            listA.add(str);
        }
        String[] strs = new String[listA.size()];
        strs=listA.toArray(strs);
        HashMap<String, String> map = new HashMap<String, String>();
        for (int i = 0; i < strs.length - 1; i = i + 2) {
            map.put(strs[i], strs[i + 1]);

        }


        System.out.println("您的字符已导入map成功");


        Set set = map.keySet();
        Iterator iter = set.iterator();
        while (iter.hasNext()) {

            String key = (String)iter.next();
            String value = (String)map.get(key);
            String strB = scB.nextLine();//接受二次输入的Key
            if (strB.equals(key)) {
                System.out.println(value);
            }



        }


        //String str = scA.nextLine();//接受一次输入的所有值
        //String[] strs = str.trim().split("");//去掉首尾空格，并用空格分割数据

    }
    public static void main(String[]args){

        testArrange();

        //先读第一排，再往下读第一排左边数字N排
        //奇数放到key里面，偶数放到value里面
        //先读取输入的数据，存储到hash表里
        //第N+2行数据与前2到N+1排的key作比较,小于k2大于K1,则取K1的v1.
        //最后一排排输出v
    }
}
*/
